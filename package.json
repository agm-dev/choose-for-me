{
  "name": "choose-for-me",
  "private": false,
  "version": "0.0.1",
  "description": "Choose random items from different categories",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "generate": "stencil generate",
    "publish": "gh-pages --dist ./www --branch master --remote ghpages"
  },
  "dependencies": {
    "@ionic/core": "^4.11.7"
  },
  "devDependencies": {
    "@stencil/core": "^1.8.4",
    "gh-pages": "^2.2.0"
  }
}
